
Процедура УстановкаПараметровСеанса(ТребуемыеПараметры)
	
	Имя = ПользователиИнформационнойБазы.ТекущийПользователь().Имя;
	
	Если Имя <> "Администратор" Тогда
		ТекПользователь = ПолучениеДанныхТекущегоСеансаСервер.ВозвратСсылкиПользователяИРесторана(Имя);
		ПараметрыСеанса.ТекущийРесторан = ТекПользователь.Ресторан;
		ПараметрыСеанса.ТекущийПользователь = ТекПользователь.Сотрудник; 
	КонецЕсли;
	
	
КонецПроцедуры
